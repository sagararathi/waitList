
  <%= form_for :user, url: login_path do |f| %>

      <%= f.text_field(:email, placeholder:"jhon@example.com", :autofocus => true)%>

      <%= f.password_field(:password, placeholder: "password") %>
      <%= f.submit('Login')%>
    <% end %>
